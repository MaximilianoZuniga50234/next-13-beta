const PostsPage = async ({ children }) => {
  return (
    <section>
      <marquee style={{ background: "#aaa", color: "black" }}>oal</marquee>
      {children}
    </section>
  );
};

export default PostsPage;
